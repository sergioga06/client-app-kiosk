/** @type {import('next').NextConfig} */
const nextConfig = {
  // Le damos permiso a la IP de tu PC para que el móvil pueda comunicarse
  allowedDevOrigins: ['192.168.1.140'],
};

export default nextConfig;